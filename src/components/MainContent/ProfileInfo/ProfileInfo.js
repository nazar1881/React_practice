import s from './ProfileInfo.module.css'

const ProfileInfo = () => {
  return (
    <div>
      <div className={s.profileDescription}>
        ava + description
      </div>
    </div>
  );
}

export default ProfileInfo;
